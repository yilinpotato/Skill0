# requirements.txt records a generic development dependency set.
# For the current ALFWorld + Qwen3 + vLLM + Ray deployment on A800 / CUDA 12.8,
# prefer `requirements-skillRL.txt` and `environment-skillRL.yml`.
accelerate
codetiming
datasets
dill
flash-attn
hydra-core
liger-kernel
numpy
pandas
peft
pyarrow>=19.0.0
pybind11
pylatexenc
pre-commit
ray[default]
tensordict==0.10.0
torchdata
transformers==4.57.3
# vllm version depends on the CUDA / torch stack used by the target machine.
wandb
packaging>=20.0
uvicorn
fastapi
qwen-vl-utils[decord]
